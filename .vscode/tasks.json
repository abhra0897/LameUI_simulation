{
	// Uses "Make" with "Makefile" to build the project.
	// Neither this nor the Makefile are auto-generated
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "cd ${workspaceFolder}; make all",
            "group": "build"
        }
    ]
}